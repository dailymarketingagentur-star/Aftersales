# Ensure shell scripts always use LF line endings (prevents "permission denied"
# errors in Docker containers when deploying from Windows)
*.sh text eol=lf

# Dockerfiles don't need a specific extension but should also be LF
Dockerfile* text eol=lf

# Keep default behavior for everything else
* text=auto
